/*
 * Translated default messages for the jQuery validation plugin.
 *
 */
jQuery.extend(jQuery.validator.messages, {
  required: localStorage.msgValidTag1,
  remote: localStorage.msgValidTag2,
  email: localStorage.msgValidTag3,
  url: localStorage.msgValidTag4,
  date: localStorage.msgValidTag5
});